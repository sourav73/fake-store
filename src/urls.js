export const baseUrl = 'https://fakestoreapi.com';
export const allProducts = `${baseUrl}/products`;
export const product = `${baseUrl}/products/`; // Concat product id with this to get the desired product
export const limitedProduct = `${baseUrl}/products?limit=`; // Concat number which is the amount of product you want to see
export const allCategories = `${baseUrl}/products/categories`;
export const category = `${baseUrl}/products/category/`; // Concat the category name you want to see
