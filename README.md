# This is a simple e-commerce project built with react,sass,react-bootstrap,redux

# Api is taken from <a href="https://fakestoreapi.com/">Here</a>

## To run the project

In the project directory run:

### `npm install` then `npm start`
